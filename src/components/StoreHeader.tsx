import { Heart, Search, ShoppingBag } from "lucide-react";
import { useState } from "react";
import CartDrawer from "./CartDrawer";
import { useShop } from "../context/ShopContext";
export default function StoreHeader() { const [open, setOpen] = useState(false); const { cartCount, wishlist } = useShop(); return <><div className="fixed right-5 md:right-12 top-24 md:top-28 z-30 flex gap-2"><a href="/#featured-collection" className="p-3 border border-white/10 bg-black/60 backdrop-blur text-white/70 hover:text-white" aria-label="Search collection"><Search size={14}/></a><a href="/#featured-collection" className="relative p-3 border border-white/10 bg-black/60 backdrop-blur text-white/70 hover:text-white" aria-label="Wishlist"><Heart size={14}/>{wishlist.length > 0 && <span className="absolute -top-1 -right-1 text-[8px] bg-white text-black rounded-full min-w-4 h-4 grid place-items-center">{wishlist.length}</span>}</a><button onClick={() => setOpen(true)} className="relative p-3 border border-white/10 bg-black/60 backdrop-blur text-white/70 hover:text-white" aria-label="Open shopping bag"><ShoppingBag size={14}/>{cartCount > 0 && <span className="absolute -top-1 -right-1 text-[8px] bg-white text-black rounded-full min-w-4 h-4 grid place-items-center">{cartCount}</span>}</button></div><CartDrawer open={open} onClose={() => setOpen(false)} /></>; }
